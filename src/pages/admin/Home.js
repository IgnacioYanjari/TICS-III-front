import React from 'react';
import { Template } from 'components';
import { admin } from 'tokens';

export default function LoginPage() {
    return (
        <Template userName="Juan Pablo Valdes" userType="admin">
            <h1>Admin Home</h1>
        </Template>
    );
}
